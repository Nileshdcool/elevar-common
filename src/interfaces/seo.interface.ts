export interface SEOProp {
    metaTitle : string,
    metaDescription : string,
    metaImage : string
}

export interface HomeSEOProps {
    metaDetails: {
      metaTitle: string;
      metaDescription: string;
      metaImage: string;
    };
  }